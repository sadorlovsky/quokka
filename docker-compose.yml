services:
  app:
    image: ghcr.io/sadorlovsky/fishka:latest
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - app-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  app-data:
